const LocationHeader = ({  }) => {
    // const {title, description, icon } = meta;
    
    return (
        <div className="ml-32 font-semibold text-lg text-white">メンバー名簿</div>
    ); 
};

export default LocationHeader;
